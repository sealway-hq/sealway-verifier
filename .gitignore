# Build output
/dist/
/bin/
# Assembled into the npm package by 'make npm-package'.
/apps/wasm/npm/sealway.wasm
/apps/wasm/npm/wasm_exec.js
/apps/wasm/npm/LICENSE
sealway-verifier
sealway-verifier.exe
# `go build ./apps/wasm` writes an executable named after the directory.
/wasm

# Test output
coverage.out
coverage.html

# Fuzzing corpora generated locally. Only minimised reproducers under
# testdata/fuzz are worth committing, and those are added deliberately.
/packages/**/testdata/fuzz/*/[0-9a-f]*

# Editor and operating system noise
.DS_Store
*.swp
.idea/
.vscode/
